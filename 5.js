
        "use strict";
             
        /**
         * @param {namber} a
         * @param {namber} b
         * @returns {namber} 
         * Функция сложения операндов
         */
        function sum(a,b){
            return a + b; 
            
        }
        let result1 = sum(10,5);
        console.log(result1);


        /**
         * @param {namber} a
         * @param {namber} b
         * @returns {namber} 
         * Функция выяетания операндов
         */
        function substruct(a,b){
            return a - b; 
        }
        let result2 = substruct(11,9);
        console.log(result2);


        /**
         * @param {namber} a
         * @param {namber} b
         * @returns {namber} 
         * Функция умножения операндов
         */
        function mul(a,b){
            return a * b; 
        }
        let result3 = mul(2,4);
        console.log(result3);

        /**
         * @param {namber} a
         * @param {namber} b
         * @returns {namber} 
         * Функция деления операндов
         */
        function div(a,b){
            return a / b; 
        }
        let result4 = div(5,1);
        console.log(result4);